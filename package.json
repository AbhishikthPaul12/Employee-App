{
  "name": "employee-app-fullstack",
  "version": "1.0.0",
  "scripts": {
    "install-backend": "cd Employee-Backend && npm install",
    "install-frontend": "cd employee-frontend && npm install",
    "build-frontend": "cd employee-frontend && npm run build",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "start": "cd Employee-Backend && npm start"
  }
}
